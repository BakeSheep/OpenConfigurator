// Compatibility export for existing web-only consumers. The generated QGC
// catalog lives in shared so both the UI and server validate the same ids.
export { getPx4AirframeInfo, PX4_AIRFRAMES } from '../../shared/airframes'
