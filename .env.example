# Optional Vite build-time settings. The application runs entirely in the
# browser; there is no local API, WebSocket, or vehicle-side server to configure.

# GitHub Pages or another subpath deployment:
# VITE_BASE_PATH=/OpenConfigurator/

# Read-only UI preview used for screenshots and browser tests:
# VITE_APP_MODE=demo
