# 安全策略

## 支持范围

OpenConfigurator 目前处于 pre-release 阶段。在首个稳定版本发布前，只维护默认分支的最新代码，不承诺为旧提交提供安全补丁。

## 报告漏洞

请不要为未修复漏洞创建公开 Issue，也不要公开可直接复现的飞控控制、鉴权绕过、MAVLink 注入或拒绝服务细节。

优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告功能。如果该功能不可用，请通过仓库所有者公开资料中提供的私密联系方式联系维护者，并仅发送最小必要信息。维护者应在公开发布前补充专用安全邮箱。

报告建议包含：

- 受影响的提交或版本、操作系统、Node.js 版本和浏览器
- 连接方式、PX4/固件版本与是否使用 MAVLink signing
- 最小复现步骤、预期与实际结果
- 影响评估，以及是否可能导致未授权控制或安全保护绕过
- 已做的脱敏说明；不要附带真实 token、signing key 或未脱敏飞行日志

维护者目标是在 7 天内确认收到报告，在完成风险评估后协调修复与披露时间。此目标不是服务等级承诺。

## 安全边界

- 默认服务仅监听 loopback。远程部署必须显式启用鉴权、精确 Origin 白名单和 HTTPS/WSS。
- 本项目的 token 保护应用访问，不能替代网络隔离、主机加固或 MAVLink 2 signing。
- UI 安全确认是纵深防御的一部分，不能替代拆桨、隔离测试区、急停和人工监督。
- 自动化测试、代码审计或漏洞修复都不构成适航认证或飞行安全保证。

部署建议与环境变量说明见 [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md)。
